general {
    colors = true
    interval = 5
}

order += "volume Master"
order += "time"

volume Master {
    format = "Vol: %volume"
    device = "pulse"
    mixer = "Master"
}

time {
    format = "%a %b %-e, %Y %-l:%M%p"
}
